---
phase: 03-multi-channel-navigation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - Sources/DankChatCore/Channels/ChannelState.swift
  - Sources/DankChatCore/Channels/ChannelStore.swift
  - Sources/DankChatApp/UI/Chat/ChatTimelineView.swift
  - Sources/DankChatApp/DankChatApp.swift
autonomous: true

must_haves:
  truths:
    - "Unread dots and mention counts update per channel"
    - "Active channel clears unread/mention indicators when opened"
    - "Switching tabs preserves scroll position per channel"
  artifacts:
    - path: "Sources/DankChatCore/Channels/ChannelState.swift"
      provides: "Unread/mention + scroll tracking per channel"
      contains: "unread"
    - path: "Sources/DankChatApp/UI/Chat/ChatTimelineView.swift"
      provides: "Scroll state reporting and restoration"
      contains: "ScrollViewReader"
  key_links:
    - from: "Sources/DankChatApp/UI/Chat/ChatTimelineView.swift"
      to: "Sources/DankChatCore/Channels/ChannelStore.swift"
      via: "scroll state updates"
      pattern: "updateScroll"
    - from: "Sources/DankChatCore/Channels/ChannelStore.swift"
      to: "Sources/DankChatApp/UI/Channels/ChannelTabStripView.swift"
      via: "unread/mention indicators"
      pattern: "unread"
---

<objective>
Add per-channel unread/mention tracking and scroll position preservation.

Purpose: Ensure multi-channel switching retains per-channel state and indicators.
Output: Unread/mention logic and scroll state wiring across channel views.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-channel-navigation/03-CONTEXT.md
@Sources/DankChatCore/Channels/ChannelStore.swift
@Sources/DankChatApp/UI/Chat/ChatTimelineView.swift
@Sources/DankChatApp/UI/Channels/ChannelTabStripView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unread/mention + scroll state tracking</name>
  <files>
Sources/DankChatCore/Channels/ChannelState.swift
Sources/DankChatCore/Channels/ChannelStore.swift
  </files>
  <action>
Extend ChannelState and ChannelStore to manage indicators and scroll state:
- Track unreadCount (dot uses count > 0), mentionCount, isAtBottom, lastReadMessageId.
- Add methods: updateScrollState(channelId:isAtBottom:lastReadMessageId:), markChannelRead(channelId:), recordIncomingEvent(channelId:isMention:).
- Unread accumulation rule: if channel is inactive OR (active but isAtBottom == false), increment unread; otherwise mark read.
- Mention rule: increment mentionCount only when a message contains the configured nick/user (use AppConfiguration.defaultNick/defaultUser tokens passed from app), case-insensitive, and only when unread would increment.
  </action>
  <verify>
ChannelState updates unread/mention counts and resets when channel becomes active.
  </verify>
  <done>
Unread/mention counts and scroll state are tracked per channel with the specified rules.
  </done>
</task>

<task type="auto">
  <name>Task 2: Preserve scroll position per channel</name>
  <files>
Sources/DankChatApp/UI/Chat/ChatTimelineView.swift
  </files>
  <action>
Update ChatTimelineView to report and restore scroll state:
- Accept bindings or callbacks for isAtBottom and lastReadMessageId (channel-specific).
- Use ScrollViewReader to restore position on appear: scroll to lastReadMessageId if available, else to bottom.
- Only auto-scroll to latest when isAtBottom is true.
- Use a PreferenceKey + GeometryReader to detect when user scrolls away from bottom and update isAtBottom/lastReadMessageId via the bindings.
Avoid calling ScrollViewProxy inside the content builder.
  </action>
  <verify>
Switching away and back to a channel returns to the prior scroll position instead of auto-jumping.
  </verify>
  <done>
Per-channel scroll position is preserved and only auto-scrolls when at bottom.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire indicator updates into the app shell</name>
  <files>
Sources/DankChatApp/DankChatApp.swift
  </files>
  <action>
Connect channel scroll state and indicators to UI:
- Pass ChannelState bindings to ChatTimelineView for the active channel.
- On channel selection change, call markChannelRead and reset mention/unread for the newly active channel.
- When new messages arrive, call ChannelStore.recordIncomingEvent with isMention derived from configured nick/user tokens.
- Ensure unread dots and mention counts are displayed in ChannelTabStripView/ChannelListView.
  </action>
  <verify>
Unread dots appear on inactive channels; opening the channel clears indicators; mentions increment separately.
  </verify>
  <done>
Per-channel indicators and scroll state are wired end-to-end across UI and store.
  </done>
</task>

</tasks>

<verification>
- Add two channels, scroll up in one, receive messages in both, verify unread dots and mention counts.
- Switch channels and confirm scroll position restoration.
</verification>

<success_criteria>
- Unread/mention indicators behave per requirements and clear on open.
- Switching channels preserves scroll position and avoids cross-channel state bleed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-channel-navigation/03-03-SUMMARY.md`
</output>
