---
phase: 03-multi-channel-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/DankChatCore/Channels/Channel.swift
  - Sources/DankChatCore/Channels/ChannelState.swift
  - Sources/DankChatCore/Channels/ChannelStore.swift
  - Sources/DankChatCore/Chat/ChatEvent.swift
  - Sources/DankChatCore/Chat/ChatMessageMapper.swift
  - Sources/DankChatCore/Chat/ChatSession.swift
autonomous: true

must_haves:
  truths:
    - "Multiple channels can exist with isolated message buffers"
    - "System messages are scoped to a channel when possible"
    - "Channel add/remove/rename updates channel list without dropping history"
  artifacts:
    - path: "Sources/DankChatCore/Channels/ChannelStore.swift"
      provides: "Channel list, per-channel chat stores, and channel state"
      contains: "final class ChannelStore"
    - path: "Sources/DankChatCore/Chat/ChatEvent.swift"
      provides: "Channel-aware system messages"
      contains: "channel"
    - path: "Sources/DankChatCore/Chat/ChatSession.swift"
      provides: "Routes IRC events into per-channel stores"
      contains: "channelStore"
  key_links:
    - from: "Sources/DankChatCore/Chat/ChatMessageMapper.swift"
      to: "Sources/DankChatCore/Chat/ChatEvent.swift"
      via: "system message channel assignment"
      pattern: "mapSystem"
    - from: "Sources/DankChatCore/Chat/ChatSession.swift"
      to: "Sources/DankChatCore/Channels/ChannelStore.swift"
      via: "append event by channel"
      pattern: "channelStore"
---

<objective>
Establish per-channel domain state and route IRC events into channel-specific buffers.

Purpose: Enable multi-channel navigation without state bleed.
Output: Channel models/store and channel-aware chat routing.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-channel-navigation/03-CONTEXT.md
@.planning/phases/03-multi-channel-navigation/03-RESEARCH.md
@Sources/DankChatCore/Chat/ChatEvent.swift
@Sources/DankChatCore/Chat/ChatMessageMapper.swift
@Sources/DankChatCore/Chat/ChatSession.swift
@Sources/DankChatCore/Chat/ChatStore.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add channel models and multi-channel store</name>
  <files>
Sources/DankChatCore/Channels/Channel.swift
Sources/DankChatCore/Channels/ChannelState.swift
Sources/DankChatCore/Channels/ChannelStore.swift
  </files>
  <action>
Create channel domain types and a ChannelStore that owns channel list ordering plus per-channel ChatStore/state.

Include:
- Channel struct (id = normalized name, displayName, pinned flag, sortOrder).
- ChannelState struct/class for unread/mention counts, scroll state (isAtBottom, lastReadMessageId), and per-channel connection state.
- ChannelStore as ObservableObject with @Published channels and activeChannelId.
- Methods: addChannel(name:), removeChannel(id:), renameChannel(id:newName:), setPinned(id:isPinned:), moveUnpinned(fromOffsets:toOffset:), setActive(id:).
- Provide store(for:) to return a per-channel ChatStore, preserving history on removal by caching stores in a dictionary keyed by channel id.
- Provide hasHistory(id:) for remove confirmation logic.

Avoid:
- Reinitializing per-channel ChatStore on selection changes.
- Storing large buffers in SceneStorage/AppStorage.
  </action>
  <verify>
ChannelStore compiles and exposes per-channel ChatStore/state accessors.
  </verify>
  <done>
ChannelStore manages channel list + per-channel stores with stable identity and keeps history on remove.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make system messages channel-aware</name>
  <files>
Sources/DankChatCore/Chat/ChatEvent.swift
Sources/DankChatCore/Chat/ChatMessageMapper.swift
  </files>
  <action>
Update SystemMessage to carry channel information when present:
- Add channel (String?) to SystemMessage.
- Add ChatEvent.channel computed property for both message and system events.
- Update ChatMessageMapper.mapSystem to parse the channel from message.params.first (normalize by stripping leading '#').
- Preserve existing timestamp behavior and mapping logic.
  </action>
  <verify>
SystemMessage includes channel and mapper assigns it for NOTICE/USERNOTICE/ROOMSTATE.
  </verify>
  <done>
System messages include channel context so events can be routed per channel.
  </done>
</task>

<task type="auto">
  <name>Task 3: Route IRC events into per-channel stores</name>
  <files>
Sources/DankChatCore/Chat/ChatSession.swift
  </files>
  <action>
Update ChatSession to work with ChannelStore instead of a single ChatStore:
- Replace the store dependency with ChannelStore.
- On mapped ChatEvent, resolve the channel id and append to the matching ChatStore via ChannelStore.store(for:).
- Keep join(channel:) and sendMessage(text:channel:) working; ensure join uses enqueueJoin for rate-limited joining.
- If a system event has no channel, skip routing (do not append to an arbitrary channel).
  </action>
  <verify>
ChatSession compiles and appends incoming events into the correct channel-specific ChatStore.
  </verify>
  <done>
IRC events are routed by channel into per-channel buffers without state bleed.
  </done>
</task>

</tasks>

<verification>
- Build succeeds (SwiftPM or Xcode build).
- Add two channels, receive messages, confirm each channel buffer updates independently.
</verification>

<success_criteria>
- ChannelStore holds multiple channels with stable per-channel ChatStore identity.
- ChatSession routes system and message events to the correct channel store.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-channel-navigation/03-01-SUMMARY.md`
</output>
