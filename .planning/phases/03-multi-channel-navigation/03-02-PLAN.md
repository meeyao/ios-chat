---
phase: 03-multi-channel-navigation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - Sources/DankChatApp/DankChatApp.swift
  - Sources/DankChatApp/UI/Channels/ChannelTabStripView.swift
  - Sources/DankChatApp/UI/Channels/ChannelListView.swift
  - Sources/DankChatApp/UI/Channels/ChannelManagementView.swift
  - Sources/DankChatApp/UI/Channels/ChannelStatusIndicator.swift
autonomous: true

must_haves:
  truths:
    - "Users can add, rename, and remove channels"
    - "Channels are displayed as navigable tabs with swipe switching"
    - "Connection state is visible per channel in the switcher"
  artifacts:
    - path: "Sources/DankChatApp/UI/Channels/ChannelTabStripView.swift"
      provides: "Scrollable tab strip with selection binding"
      contains: "ChannelTabStripView"
    - path: "Sources/DankChatApp/DankChatApp.swift"
      provides: "App wiring for multi-channel navigation"
      contains: "ChannelStore"
  key_links:
    - from: "Sources/DankChatApp/UI/Channels/ChannelListView.swift"
      to: "Sources/DankChatCore/Channels/ChannelStore.swift"
      via: "binding and actions"
      pattern: "ChannelStore"
    - from: "Sources/DankChatApp/DankChatApp.swift"
      to: "Sources/DankChatApp/UI/Channels/ChannelTabStripView.swift"
      via: "selection binding"
      pattern: "selection"
---

<objective>
Deliver the multi-channel navigation UI with channel management flows and tabbed switching.

Purpose: Let users move between channels and manage their list without losing state.
Output: Channel switcher UI, management screen, and app shell wiring.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-channel-navigation/03-CONTEXT.md
@.planning/phases/03-multi-channel-navigation/03-RESEARCH.md
@Sources/DankChatApp/DankChatApp.swift
@Sources/DankChatApp/UI/Chat/ChatTimelineView.swift
@Sources/DankChatApp/UI/Chat/ChatComposerView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build channel switcher UI</name>
  <files>
Sources/DankChatApp/UI/Channels/ChannelTabStripView.swift
Sources/DankChatApp/UI/Channels/ChannelListView.swift
Sources/DankChatApp/UI/Channels/ChannelStatusIndicator.swift
  </files>
  <action>
Create channel switcher components:
- ChannelTabStripView: horizontal ScrollView of tabs with selection binding, channel name only, pinned ordering respected, swipe left/right supported via the container (TabView uses .page style).
- ChannelListView: sidebar list for iPad (NavigationSplitView), showing same channels with unread/mention indicators and connection status icon.
- ChannelStatusIndicator: icon-only indicator for Connected/Connecting/Reconnecting/Disconnected.
- Provide context menu actions per channel: Pin/Unpin, Rename, Remove, Reconnect now.

Follow decisions:
- Keep switcher scrollable (no overflow menu).
- Show channel name only in switcher.
- Include per-channel status indicator in switcher.
  </action>
  <verify>
Tabs and list render, selection highlights active channel, and status icons display for each channel.
  </verify>
  <done>
Channel switcher UI exists for iPhone (tab strip) and iPad (sidebar list) with status indicators.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement channel management flows</name>
  <files>
Sources/DankChatApp/UI/Channels/ChannelManagementView.swift
Sources/DankChatApp/UI/Channels/ChannelTabStripView.swift
Sources/DankChatApp/UI/Channels/ChannelListView.swift
  </files>
  <action>
Add join/rename/remove/reorder flows:
- Join: + button opens prompt, accepts name with or without '#', normalizes, calls ChannelStore.addChannel and ChatSession.join immediately.
- Remove: confirm only when ChannelStore.hasHistory is true; removal keeps cached history by default.
- Rename: prompt for new name, replaces channel in place (same slot), calls PART on old and JOIN on new.
- Pinning: toggle pin in context menu; pinned channels stay first.
- Reordering: use .onMove for unpinned channels (pinned order remains fixed).
- Provide a dedicated management view (list + edit controls) reachable from the switcher.
  </action>
  <verify>
Join, rename, remove, pin, and reorder actions update channel list and keep history as configured.
  </verify>
  <done>
Users can manage channels with the defined UX and semantics from phase context.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire multi-channel navigation into app shell</name>
  <files>
Sources/DankChatApp/DankChatApp.swift
  </files>
  <action>
Replace the single-channel layout with a multi-channel container:
- Create a ChannelStore @StateObject and pass it to channel views and ChatSession.
- Use @SceneStorage("activeChannelId") to restore the last active channel on launch.
- iPhone: TabView(selection:) with .page style + ChannelTabStripView; each tab shows ChatTimelineView + ChatComposerView for that channel's ChatStore.
- iPad: NavigationSplitView with collapsible sidebar (ChannelListView) and detail showing the active channel content.
- Empty state when no channels: show prompt to add a channel.
- Keep existing auth/connect controls accessible (header or sidebar section).
  </action>
  <verify>
App launches with last active channel selected; switching tabs preserves channel-specific chat buffers.
  </verify>
  <done>
Multi-channel navigation replaces the single-channel shell on iPhone and iPad.
  </done>
</task>

</tasks>

<verification>
- Run the app in Xcode, add two channels, and swipe between them.
- Confirm iPad sidebar can be collapsed and channel list remains functional.
</verification>

<success_criteria>
- Channel switcher and management flows are usable on iPhone/iPad.
- Tabs/list show per-channel status indicators and switching preserves buffers.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-channel-navigation/03-02-SUMMARY.md`
</output>
