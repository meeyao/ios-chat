---
phase: 05-social-moderation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - Sources/DankChatCore/Chat/ChatMessage.swift
  - Sources/DankChatCore/Chat/ChatMessageMapper.swift
  - Sources/DankChatCore/Social/SocialMessageClassifier.swift
  - Sources/DankChatCore/Social/SocialTabStore.swift
  - Sources/DankChatApp/DankChatApp.swift
  - Sources/DankChatApp/UI/Chat/ChatMessageRow.swift
  - Sources/DankChatApp/UI/Chat/ReplyThreadView.swift
  - Sources/DankChatApp/UI/Tabs/MentionsTabView.swift
  - Sources/DankChatApp/UI/Tabs/RepliesTabView.swift
  - Sources/DankChatApp/UI/Chat/ChatTimelineView.swift
autonomous: true

must_haves:
  truths:
    - "Reply metadata is available for reply messages"
    - "Reply threads can be opened to view parent context"
    - "Mentions and replies appear in dedicated tabs"
    - "Mention counts reflect the signed-in user identity"
  artifacts:
    - path: "Sources/DankChatCore/Chat/ChatMessage.swift"
      provides: "Reply metadata on chat messages"
    - path: "Sources/DankChatCore/Social/SocialTabStore.swift"
      provides: "Mention/reply stores for tabs"
    - path: "Sources/DankChatApp/UI/Chat/ReplyThreadView.swift"
      provides: "Reply thread UI for parent context"
    - path: "Sources/DankChatApp/UI/Tabs/MentionsTabView.swift"
      provides: "Mentions tab UI"
    - path: "Sources/DankChatApp/UI/Tabs/RepliesTabView.swift"
      provides: "Replies tab UI"
  key_links:
    - from: "Sources/DankChatCore/Chat/ChatMessageMapper.swift"
      to: "Sources/DankChatCore/Chat/ChatMessage.swift"
      via: "reply tag parsing"
      pattern: "reply-parent"
    - from: "Sources/DankChatApp/DankChatApp.swift"
      to: "Sources/DankChatCore/Social/SocialTabStore.swift"
      via: "append incoming events"
      pattern: "SocialTabStore"
    - from: "Sources/DankChatCore/Social/SocialMessageClassifier.swift"
      to: "Sources/DankChatCore/Social/UserIdentityStore.swift"
      via: "current user login/displayName"
      pattern: "UserIdentityStore"
    - from: "Sources/DankChatApp/UI/Chat/ChatMessageRow.swift"
      to: "Sources/DankChatApp/UI/Chat/ReplyThreadView.swift"
      via: "reply thread presentation"
      pattern: "ReplyThreadView"
---

<objective>
Add reply metadata parsing and create Mentions/Replies tabs backed by a social store that classifies incoming messages for the current user.

Purpose: Mentions and replies are core social surfaces in DankChat; they must mirror message metadata and reflect the signed-in identity.
Output: Reply metadata on messages, a classifier + stores, and UI tabs to browse mentions/replies.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-social-moderation/05-RESEARCH.md

@Sources/DankChatCore/Chat/ChatMessage.swift
@Sources/DankChatCore/Chat/ChatMessageMapper.swift
@Sources/DankChatApp/DankChatApp.swift
@Sources/DankChatApp/UI/Chat/ChatTimelineView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse reply tags into chat message metadata</name>
  <files>Sources/DankChatCore/Chat/ChatMessage.swift, Sources/DankChatCore/Chat/ChatMessageMapper.swift</files>
  <action>
Extend `ChatMessage` with a `ReplyMetadata` struct containing:
- parent message id
- parent user id/login/display name
- parent message body

Update `ChatMessageMapper` to populate reply metadata from IRC tags:
`reply-parent-msg-id`, `reply-parent-user-id`, `reply-parent-user-login`, `reply-parent-display-name`, `reply-parent-msg-body`.

Constraints:
- Keep reply metadata optional (only present when tags exist).
- Do not break existing `Equatable` behavior.
  </action>
  <verify>
Build: `swift build`.
  </verify>
  <done>
Reply tags appear on `ChatMessage` when IRC reply tags are present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add social classifier and mention/reply stores</name>
  <files>Sources/DankChatCore/Social/SocialMessageClassifier.swift, Sources/DankChatCore/Social/SocialTabStore.swift, Sources/DankChatApp/DankChatApp.swift</files>
  <action>
Create `SocialMessageClassifier` with methods:
- `isMention(message: ChatMessage, identity: HelixUser?) -> Bool`
- `isReply(message: ChatMessage, identity: HelixUser?) -> Bool`

Create `SocialTabStore` (ObservableObject) that keeps two `ChatStore`s:
- `mentionsStore`
- `repliesStore`

Add a method `append(event: ChatEvent, identity: HelixUser?)` that uses the classifier to add events to the appropriate stores.

Wire `SocialTabStore` in `ContentView`:
- Create `StateObject` and inject via `.environmentObject`.
- On incoming events (where `recordIncomingEvent` is called), also forward to `SocialTabStore.append` with the current identity.
- Update mention counting to use classifier + identity instead of `defaultNick/defaultUser`.
  </action>
  <verify>
Build: `swift build`.
  </verify>
  <done>
Mentions/replies stores update as events arrive, and mention counts reflect authenticated identity.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Mentions/Replies tabs and reply thread UI</name>
  <files>Sources/DankChatApp/UI/Tabs/MentionsTabView.swift, Sources/DankChatApp/UI/Tabs/RepliesTabView.swift, Sources/DankChatApp/DankChatApp.swift, Sources/DankChatApp/UI/Chat/ChatTimelineView.swift, Sources/DankChatApp/UI/Chat/ReplyThreadView.swift, Sources/DankChatApp/UI/Chat/ChatMessageRow.swift</files>
  <action>
Create `MentionsTabView` and `RepliesTabView` that render `ChatTimelineView` with `SocialTabStore.mentionsStore` and `SocialTabStore.repliesStore`.

Update `ContentView.channelDetail` to add a lightweight tab/segment control with three options: Chat, Mentions, Replies. When switching:
- Chat uses the channel store as today.
- Mentions uses `mentionsStore`.
- Replies uses `repliesStore`.

Ensure the tab control is compact-friendly and does not break iPad layout.

Add a `ReplyThreadView` that displays the parent message context and the replied-to message when `ChatMessage.replyMetadata` is present.

Update `ChatMessageRow` to present `ReplyThreadView` (sheet or popover) from a reply indicator or context action, only when reply metadata exists.
  </action>
  <verify>
Build: `swift build`.
  </verify>
  <done>
Mentions/Replies tabs render message timelines and reply threads open from replied messages.
  </done>
</task>

</tasks>

<verification>
- `swift build` succeeds.
  - Mentions/Replies tabs render and update as messages arrive.
  - Reply thread UI opens and shows parent context for reply messages.
</verification>

<success_criteria>
  - Reply metadata is captured, reply threads open with parent context, and social tabs show the correct messages.
</success_criteria>

<output>
After completion, create `.planning/phases/05-social-moderation/05-02-SUMMARY.md`.
</output>
