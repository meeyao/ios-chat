---
phase: 05-social-moderation
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - Sources/DankChatCore/Services/Helix/HelixUserProfileService.swift
  - Sources/DankChatCore/Services/Helix/HelixFollowageService.swift
  - Sources/DankChatCore/Services/Helix/HelixBlockService.swift
  - Sources/DankChatCore/Social/UserProfileStore.swift
  - Sources/DankChatApp/UI/UserPopup/UserPopupView.swift
  - Sources/DankChatApp/UI/Chat/ChatMessageRow.swift
autonomous: true

must_haves:
  truths:
    - "Tapping a username opens a user popup with profile and followage"
    - "Block/unblock actions are available when scopes permit"
  artifacts:
    - path: "Sources/DankChatApp/UI/UserPopup/UserPopupView.swift"
      provides: "User popup UI for profile and actions"
    - path: "Sources/DankChatCore/Services/Helix/HelixUserProfileService.swift"
      provides: "Helix Get Users wrapper"
    - path: "Sources/DankChatCore/Services/Helix/HelixFollowageService.swift"
      provides: "Helix Get Channel Followers wrapper"
    - path: "Sources/DankChatCore/Services/Helix/HelixBlockService.swift"
      provides: "Helix block/unblock user wrappers"
  key_links:
    - from: "Sources/DankChatApp/UI/Chat/ChatMessageRow.swift"
      to: "Sources/DankChatApp/UI/UserPopup/UserPopupView.swift"
      via: "tap gesture or context menu"
      pattern: "UserPopupView"
    - from: "Sources/DankChatApp/UI/UserPopup/UserPopupView.swift"
      to: "Sources/DankChatCore/Services/Helix/HelixUserProfileService.swift"
      via: "load profile"
      pattern: "HelixUserProfileService"
    - from: "Sources/DankChatCore/Social/UserProfileStore.swift"
      to: "Sources/DankChatCore/Services/Helix/HelixBlockService.swift"
      via: "block/unblock actions"
      pattern: "HelixBlockService"
---

<objective>
Add a user popup with profile, followage, and block controls, connected to Helix services and triggered from chat messages.

Purpose: User actions are central to social workflows; users expect quick access to profile details and block management.
Output: User popup UI, Helix profile/followage/block services, and ChatMessageRow integration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-social-moderation/05-RESEARCH.md

@Sources/DankChatApp/UI/Chat/ChatMessageRow.swift
@Sources/DankChatCore/Social/UserIdentityStore.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Helix user profile/followage/block services</name>
  <files>Sources/DankChatCore/Services/Helix/HelixUserProfileService.swift, Sources/DankChatCore/Services/Helix/HelixFollowageService.swift, Sources/DankChatCore/Services/Helix/HelixBlockService.swift, Sources/DankChatCore/Social/UserProfileStore.swift</files>
  <action>
Create Helix services for:
- Get user profile (`GET /users`)
- Get followage (`GET /channels/followers`) gated by `moderator:read:followers`
- Block/unblock (`PUT /users/blocks`, `DELETE /users/blocks`) gated by `user:manage:blocked_users`

Add `UserProfileStore` to load and cache profile/followage for a selected user, plus block state.
Expose `blockUser()` and `unblockUser()` methods that call `HelixBlockService`, update cached block state on success, and surface errors for the UI.
  </action>
  <verify>
Build: `swift build`.
  </verify>
  <done>
Services compile and `UserProfileStore` exposes profile, followage, and block state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add user popup UI and hook into chat rows</name>
  <files>Sources/DankChatApp/UI/UserPopup/UserPopupView.swift, Sources/DankChatApp/UI/Chat/ChatMessageRow.swift</files>
  <action>
Create `UserPopupView` that shows:
- Display name, login, avatar
- Followage (or “unknown” if scope missing)
- Block/unblock button with error state if scope missing

Update `ChatMessageRow` to present `UserPopupView` on username tap (sheet or popover). Pass the user id/login/display name to `UserProfileStore` for loading.
Wire the block/unblock button to `UserProfileStore.blockUser()`/`unblockUser()` and reflect the updated block state in the popup.
  </action>
  <verify>
Build: `swift build`.
  </verify>
  <done>
User popup opens from chat rows and displays profile/followage/block actions.
  </done>
</task>

</tasks>

<verification>
- `swift build` succeeds.
- Username taps open a user popup with profile and followage.
</verification>

<success_criteria>
- User popup actions reflect Helix permissions and state.
</success_criteria>

<output>
After completion, create `.planning/phases/05-social-moderation/05-04-SUMMARY.md`.
</output>
