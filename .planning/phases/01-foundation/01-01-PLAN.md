---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/DankChatCore/Models/OAuthToken.swift
  - Sources/DankChatCore/Services/Auth/OAuthConfiguration.swift
  - Sources/DankChatCore/Services/Auth/OAuthPKCE.swift
  - Sources/DankChatCore/Services/Auth/OAuthClient.swift
  - Sources/DankChatCore/Services/Auth/OAuthAuthorizing.swift
  - Sources/DankChatCore/Services/Auth/OAuthTokenStore.swift
  - Sources/DankChatCore/Services/Auth/KeychainTokenStore.swift
  - Sources/DankChatCore/Services/Auth/AuthManager.swift
  - Sources/DankChatApp/AppConfiguration.swift
  - Sources/DankChatApp/Resources/Config.plist
  - Sources/DankChatApp/DankChatApp.swift
autonomous: true
user_setup:
  - service: twitch
    why: "OAuth client credentials for user login"
    env_vars:
      - name: TWITCH_CLIENT_ID
        source: "Twitch Developer Console -> Applications -> Client ID"
    dashboard_config:
      - task: "Register app redirect URI for ASWebAuthenticationSession"
        location: "Twitch Developer Console -> Applications -> Redirect URLs"

must_haves:
  truths:
    - "User can initiate Twitch OAuth login from the app"
    - "OAuth tokens persist securely and restore on relaunch"
    - "Expired tokens refresh or fall back to signed-out state"
  artifacts:
    - path: "Sources/DankChatCore/Services/Auth/AuthManager.swift"
      provides: "Auth state machine and login/refresh flow"
    - path: "Sources/DankChatCore/Services/Auth/OAuthClient.swift"
      provides: "OAuth URL building and token exchange"
    - path: "Sources/DankChatCore/Services/Auth/KeychainTokenStore.swift"
      provides: "Keychain-backed token persistence"
    - path: "Sources/DankChatApp/AppConfiguration.swift"
      provides: "OAuth/client configuration loading"
    - path: "Sources/DankChatApp/Resources/Config.plist"
      provides: "Client id/redirect configuration source"
  key_links:
    - from: "Sources/DankChatApp/DankChatApp.swift"
      to: "Sources/DankChatCore/Services/Auth/AuthManager.swift"
      via: "ContentView sign-in buttons"
      pattern: "beginLogin\\(\\)"
    - from: "Sources/DankChatCore/Services/Auth/AuthManager.swift"
      to: "Sources/DankChatCore/Services/Auth/OAuthClient.swift"
      via: "authorizationURL/exchange/refresh calls"
      pattern: "authorizationURL|exchange\\(|refresh\\("
    - from: "Sources/DankChatCore/Services/Auth/AuthManager.swift"
      to: "Sources/DankChatCore/Services/Auth/KeychainTokenStore.swift"
      via: "read/write token"
      pattern: "readToken\\(|writeToken\\("
---

<objective>
Implement Twitch OAuth login, token persistence, and app wiring.

Purpose: Enable user authentication and secure session restoration for chat.
Output: OAuth services, Keychain token store, and app configuration wiring.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Sources/DankChatCore/Services/Auth/AuthManager.swift
@Sources/DankChatCore/Services/Auth/OAuthClient.swift
@Sources/DankChatCore/Services/Auth/KeychainTokenStore.swift
@Sources/DankChatApp/AppConfiguration.swift
@Sources/DankChatApp/DankChatApp.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build OAuth configuration + client exchange flow</name>
  <files>Sources/DankChatCore/Services/Auth/OAuthConfiguration.swift, Sources/DankChatCore/Services/Auth/OAuthPKCE.swift, Sources/DankChatCore/Services/Auth/OAuthClient.swift, Sources/DankChatCore/Models/OAuthToken.swift</files>
  <action>Define OAuthConfiguration with client id, redirect URI, scopes, and Twitch endpoints; implement PKCE helper (verifier + S256 challenge) and OAuthClient methods for authorization URL, token exchange, and refresh; model OAuthToken with expiry timestamps and helpers (isExpired, shouldRefresh) using date-based logic.</action>
  <verify>swift test</verify>
  <done>Authorization URL uses PKCE + scope, token exchange/refresh decodes Twitch responses into OAuthToken with expiry helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Persist OAuth tokens in Keychain</name>
  <files>Sources/DankChatCore/Services/Auth/OAuthTokenStore.swift, Sources/DankChatCore/Services/Auth/KeychainTokenStore.swift</files>
  <action>Create OAuthTokenStore protocol and KeychainTokenStore implementation that saves, reads, updates, and clears OAuthToken using JSON encoding with ISO8601 dates; handle not-found vs error cases cleanly.</action>
  <verify>swift test</verify>
  <done>Tokens written to Keychain are readable on relaunch and clear without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Wire OAuth into the app shell</name>
  <files>Sources/DankChatApp/AppConfiguration.swift, Sources/DankChatApp/Resources/Config.plist, Sources/DankChatApp/DankChatApp.swift, Sources/DankChatCore/Services/Auth/AuthManager.swift, Sources/DankChatCore/Services/Auth/OAuthAuthorizing.swift</files>
  <action>Load OAuth client configuration from Config.plist, initialize AuthManager with OAuthClient + KeychainTokenStore + SystemOAuthAuthorizer, and surface sign-in/sign-out actions in the app shell; ensure restoreSession and refreshIfNeeded can be triggered on app start without blocking UI.</action>
  <verify>swift test</verify>
  <done>App can initiate OAuth login, store token, and restore session on relaunch via AuthManager state.</done>
</task>

</tasks>

<verification>
- swift test
- Manual run: tap Sign In and complete OAuth flow, relaunch app and confirm session restores.
</verification>

<success_criteria>
- AuthManager transitions through signed-out → signing-in → signed-in with persisted tokens.
- Keychain token storage survives app relaunch and clears on sign out.
- Config-driven OAuth settings load from Config.plist without hardcoding secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
