---
phase: 04-emotes-badges-ecosystem
plan: 06
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - Sources/DankChatApp/UI/Chat/ChatComposerView.swift
  - Sources/DankChatApp/UI/Emotes/EmoteMenuSheet.swift
  - Sources/DankChatApp/UI/Emotes/EmoteGridView.swift
  - Sources/DankChatCore/Emotes/EmoteRecentsStore.swift
  - Sources/DankChatCore/Emotes/EmoteMenuSettings.swift
autonomous: true

must_haves:
  truths:
    - "User can open an emote menu bottom sheet and insert an emote into the composer without sending"
    - "Menu has tabs, always-visible search, and a recents list limited to ~20"
  artifacts:
    - path: "Sources/DankChatApp/UI/Emotes/EmoteMenuSheet.swift"
      provides: "Bottom sheet emote menu with tabs + search"
    - path: "Sources/DankChatCore/Emotes/EmoteRecentsStore.swift"
      provides: "Recents tracking (sent + tapped), limited and persisted"
  key_links:
    - from: "Sources/DankChatApp/UI/Chat/ChatComposerView.swift"
      to: "Sources/DankChatApp/UI/Emotes/EmoteMenuSheet.swift"
      via: ".sheet presentation"
      pattern: "sheet\\(isPresented:"
---

<objective>
Implement the emote menu UX: composer button opens a bottom sheet with provider tabs, search, and recents, and inserts selected emotes into the composer text.

Purpose: Users need a fast emote picker matching DankChat behavior.
Output: Emote menu sheet is integrated into `ChatComposerView` and uses `EmoteStore` data.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-emotes-badges-ecosystem/04-CONTEXT.md

@Sources/DankChatApp/UI/Chat/ChatComposerView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement recents store + ordering setting</name>
  <files>Sources/DankChatCore/Emotes/EmoteRecentsStore.swift, Sources/DankChatCore/Emotes/EmoteMenuSettings.swift</files>
  <action>
Create `EmoteRecentsStore` that:
- Tracks recents from both sent emotes and tapped emotes from the menu.
- Enforces a practical limit (~20).
- Persists across app restarts.

Create `EmoteMenuSettings` that includes the user-facing ordering toggle:
- `most recent` vs `most used`.

Default ordering is OpenCode's discretion unless specified elsewhere.
  </action>
  <verify>Run: `swift build`</verify>
  <done>Recents store compiles, persists, and can return ordered recents.</done>
</task>

<task type="auto">
  <name>Task 2: Build the emote menu bottom sheet with tabs + always-visible search</name>
  <files>Sources/DankChatApp/UI/Emotes/EmoteMenuSheet.swift, Sources/DankChatApp/UI/Emotes/EmoteGridView.swift</files>
  <action>
Implement `EmoteMenuSheet` as a bottom sheet that:
- Opens half-height and supports drag to full height.
- Shows tabs in this order (locked): `Recents, Twitch, 7TV, BTTV, FFZ, Emoji`.
- Defaults to `Recents` on open.
- Has an always-visible search field.

Implement `EmoteGridView` to efficiently render large emote lists (1000+), with smooth scrolling and selection.
  </action>
  <verify>Run: `swift build`</verify>
  <done>Menu sheet compiles and can display emote lists per tab with search filtering.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate menu into composer with an always-visible emote button</name>
  <files>Sources/DankChatApp/UI/Chat/ChatComposerView.swift</files>
  <action>
Update `ChatComposerView`:
- Add an emote button that is always visible.
- Present `EmoteMenuSheet` via `.sheet`.
- On emote selection: insert into the composer text at cursor and do NOT send the message.

Ensure Return/Enter is still the only send trigger.
  </action>
  <verify>
Run: `swift build`.
Manual run: opening menu and selecting an emote inserts text and does not send.
  </verify>
  <done>
Emote menu is usable end-to-end and respects the non-send insertion rule.
  </done>
</task>

</tasks>

<verification>
- Manual run: sheet opens as bottom sheet; tabs order matches context; search field always visible; recents capped around 20.
</verification>

<success_criteria>
- Emote menu UX matches the locked decisions sufficiently for Phase 4 planning completion.
</success_criteria>

<output>
After completion, create `.planning/phases/04-emotes-badges-ecosystem/04-06-SUMMARY.md`.
</output>
