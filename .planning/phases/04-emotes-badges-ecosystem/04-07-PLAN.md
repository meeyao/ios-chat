---
phase: 04-emotes-badges-ecosystem
plan: 07
type: execute
wave: 5
depends_on: ["04-06"]
files_modified:
  - Sources/DankChatCore/Emotes/Suggestions/EmoteSuggestionEngine.swift
  - Sources/DankChatCore/Emotes/Suggestions/EmoteTokenization.swift
  - Sources/DankChatApp/UI/Chat/ChatComposerView.swift
  - Sources/DankChatApp/UI/Emotes/EmoteSuggestionsView.swift
autonomous: true

must_haves:
  truths:
    - "Emote suggestions appear for ':' and word-prefix typing and can be accepted via tap"
    - "Accepting a suggestion replaces the current token and inserts a trailing space without sending"
  artifacts:
    - path: "Sources/DankChatCore/Emotes/Suggestions/EmoteSuggestionEngine.swift"
      provides: "Prefix + fuzzy suggestion ranking"
    - path: "Sources/DankChatApp/UI/Emotes/EmoteSuggestionsView.swift"
      provides: "3-5 item suggestions UI anchored to composer"
  key_links:
    - from: "Sources/DankChatApp/UI/Chat/ChatComposerView.swift"
      to: "Sources/DankChatCore/Emotes/Suggestions/EmoteSuggestionEngine.swift"
      via: "compose suggestions from EmoteStore"
      pattern: "EmoteSuggestionEngine"
---

<objective>
Implement emote suggestions in the composer with fuzzy matching, correct trigger rules, and DankChat-like insert behavior.

Purpose: Suggestions speed up chat; behavior details are locked in Phase 4 context.
Output: Suggestions UI + engine wired into `ChatComposerView`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-emotes-badges-ecosystem/04-CONTEXT.md
@Sources/DankChatApp/UI/Chat/ChatComposerView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement tokenization + suggestion engine (prefix + fuzzy)</name>
  <files>Sources/DankChatCore/Emotes/Suggestions/EmoteTokenization.swift, Sources/DankChatCore/Emotes/Suggestions/EmoteSuggestionEngine.swift</files>
  <action>
Implement a small core suggestion module that:
- Extracts the current token at the cursor.
- Supports trigger rules locked by context:
  - `:`-prefixed suggestions
  - normal word-prefix suggestions
- Produces ranked suggestions using prefix + fuzzy matching.

The engine should accept the current `EmoteStore` index (all providers mixed) and return top-N suggestions.
  </action>
  <verify>Run: `swift build`</verify>
  <done>Engine compiles and can return ranked suggestions for a given input string + cursor position.</done>
</task>

<task type="auto">
  <name>Task 2: Implement suggestions UI (3-5 items) and wire into composer</name>
  <files>Sources/DankChatApp/UI/Emotes/EmoteSuggestionsView.swift, Sources/DankChatApp/UI/Chat/ChatComposerView.swift</files>
  <action>
Add `EmoteSuggestionsView` and integrate into `ChatComposerView`.

UI/behavior locked by context:
- Show 3-5 suggestions.
- Dismiss automatically on whitespace.
- Accept suggestion via tap.
- Accepting replaces the current token and inserts a trailing space.
- Accepting does NOT send the message (send only on Return/Enter).

Ensure suggestions can coexist with the emote menu button from Plan 04-06.
  </action>
  <verify>
Run: `swift build`.
Manual run: type `:ka` or `kapp` and see suggestions; tapping inserts and does not send.
  </verify>
  <done>
Suggestions behave according to locked rules and do not interfere with send behavior.
  </done>
</task>

</tasks>

<verification>
- Manual run: suggestions show for ':' and word-prefix; tap inserts with trailing space; whitespace dismisses; Return sends only.
</verification>

<success_criteria>
- Composer suggestions reach parity on the key locked behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/04-emotes-badges-ecosystem/04-07-SUMMARY.md`.
</output>
