---
phase: 04-emotes-badges-ecosystem
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - Sources/DankChatApp/UI/Chat/ChatMessageRow.swift
  - Sources/DankChatApp/UI/Chat/RichText/ChatRichTextView.swift
  - Sources/DankChatApp/UI/Chat/RichText/ChatRichTextBuilder.swift
  - Sources/DankChatCore/Emotes/EmoteStore.swift
  - Sources/DankChatCore/Chat/ChatSettings.swift
  - Sources/DankChatApp/UI/Chat/ChatSettingsView.swift
autonomous: true

must_haves:
  truths:
    - "Chat messages render inline emotes with correct wrapping and spacing rules"
    - "Animated emotes can be disabled via a setting"
  artifacts:
    - path: "Sources/DankChatApp/UI/Chat/RichText/ChatRichTextView.swift"
      provides: "UIKit-backed rich text renderer for inline emotes"
    - path: "Sources/DankChatApp/UI/Chat/RichText/ChatRichTextBuilder.swift"
      provides: "Build attributed text with inline emote attachments"
  key_links:
    - from: "Sources/DankChatApp/UI/Chat/ChatMessageRow.swift"
      to: "Sources/DankChatApp/UI/Chat/RichText/ChatRichTextView.swift"
      via: "SwiftUI wrapper"
      pattern: "ChatRichTextView"
---

<objective>
Replace the SwiftUI-only message text with a UIKit-backed renderer that can place inline emotes with DankChat-like spacing rules.

Purpose: SwiftUI `Text(AttributedString)` cannot host inline animated/cached emotes. A dedicated renderer enables baseline-ish inline emotes and precise wrapping.
Output: `ChatMessageRow` uses `ChatRichTextView` for message text, powered by `EmoteStore`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-emotes-badges-ecosystem/04-CONTEXT.md
@.planning/phases/04-emotes-badges-ecosystem/04-RESEARCH.md

@Sources/DankChatApp/UI/Chat/ChatMessageRow.swift
@Sources/DankChatCore/Chat/ChatSettings.swift
@Sources/DankChatApp/UI/Chat/ChatSettingsView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add emote animation toggle to settings</name>
  <files>Sources/DankChatCore/Chat/ChatSettings.swift, Sources/DankChatApp/UI/Chat/ChatSettingsView.swift</files>
  <action>
Add a new setting in `ChatSettings` for emote animation control (e.g. `animateEmotes` / `allowAnimatedEmotes`).

Expose it in `ChatSettingsView` as a toggle. Default should allow animations (parity-friendly), while enabling a user option to disable.
  </action>
  <verify>Run: `swift build`</verify>
  <done>Settings compile and the toggle appears in the settings UI code path.</done>
</task>

<task type="auto">
  <name>Task 2: Implement rich text builder for inline emotes (spacing + wrap rules)</name>
  <files>Sources/DankChatApp/UI/Chat/RichText/ChatRichTextBuilder.swift, Sources/DankChatCore/Emotes/EmoteStore.swift</files>
  <action>
Create `ChatRichTextBuilder` to produce an `NSAttributedString` for a chat message.

Requirements (locked by context):
- Emote size is DankChat-like (larger than the surrounding text).
- Collapse a single space between adjacent emotes: `emote + space + emote`.
- Allow wrapping between emotes even when space-collapsing is enabled.

Implementation guidance:
- Use `NSTextAttachment` for inline emote placeholders.
- Use `EmoteStore` to resolve:
  - Twitch emotes using parsed `ChatMessage.twitchEmotes` ranges
  - Third-party emotes by matching codes in tokens

If animation is disabled, render a static frame (or prefer a static format) for animated emotes.
  </action>
  <verify>Run: `swift build`</verify>
  <done>Builder compiles and produces attributed strings with inline emote attachments per the spacing/wrap rules.</done>
</task>

<task type="auto">
  <name>Task 3: Render message text via UIKit-backed view in ChatMessageRow</name>
  <files>Sources/DankChatApp/UI/Chat/RichText/ChatRichTextView.swift, Sources/DankChatApp/UI/Chat/ChatMessageRow.swift</files>
  <action>
Create `ChatRichTextView` as a `UIViewRepresentable` wrapping a non-editable text view (or custom TextKit 2 view) that can display `NSAttributedString`.

Update `ChatMessageRow` to replace `Text(attributed)` with `ChatRichTextView`.

Preserve existing behavior:
- Link handling remains functional (tap opens as before, or uses `UITextView` link interaction).
- Action messages remain italicized.

Pass in `ChatSettings` (for animation toggle) and `EmoteStore` (for resolution).
  </action>
  <verify>Run: `swift build`</verify>
  <done>Chat message rows compile and use the new rich renderer for message text.</done>
</task>

</tasks>

<verification>
- `swift build` succeeds.
- Manual run in Xcode: a message containing emote codes renders inline images; adjacent emotes collapse a single space; message still wraps.
</verification>

<success_criteria>
- Inline emote rendering is in place and respects the core spacing/wrapping decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-emotes-badges-ecosystem/04-04-SUMMARY.md`.
</output>
