---
phase: 04-emotes-badges-ecosystem
plan: 08
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - Sources/DankChatApp/UI/Chat/ProviderOutageBannerView.swift
  - Sources/DankChatApp/DankChatApp.swift
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Provider outages surface as a non-blocking banner/toast while cached emotes/badges continue to render"
  artifacts:
    - path: "Sources/DankChatApp/UI/Chat/ProviderOutageBannerView.swift"
      provides: "Outage banner UI bound to ProviderStatusStore"
    - path: "Sources/DankChatApp/DankChatApp.swift"
      provides: "Chat layout wiring for outage banner"
  key_links:
    - from: "Sources/DankChatApp/UI/Chat/ProviderOutageBannerView.swift"
      to: "Sources/DankChatCore/Services/ProviderStatusStore.swift"
      via: "EnvironmentObject outages observer"
      pattern: "providerStatusStore\\.outages"
    - from: "Sources/DankChatApp/DankChatApp.swift"
      to: "Sources/DankChatApp/UI/Chat/ProviderOutageBannerView.swift"
      via: "channelDetail layout"
      pattern: "ProviderOutageBannerView"
---

<objective>
Surface provider outage events as a non-blocking banner/toast in the chat UI.

Purpose: Close the gap where outages are recorded but never surfaced to users.
Output: A banner view that observes outages and a wiring point in the chat layout.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-emotes-badges-ecosystem/04-VERIFICATION.md
@.planning/phases/04-emotes-badges-ecosystem/04-CONTEXT.md
@Sources/DankChatCore/Services/ProviderStatusStore.swift
@Sources/DankChatApp/DankChatApp.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider outage banner component</name>
  <files>Sources/DankChatApp/UI/Chat/ProviderOutageBannerView.swift</files>
  <action>
Create a small SwiftUI banner/toast view that observes provider outages via `ProviderStatusStore`.
- Use `@EnvironmentObject var providerStatusStore: ProviderStatusStore` and display the most recent outage.
- Show provider display name + message (copy can be brief and neutral).
- Animate in/out and auto-dismiss after a short delay, but keep updates non-blocking (no modal UI).
- Avoid retaining a growing list in the view; only surface the newest outage event.
  </action>
  <verify>Run: `swift build`</verify>
  <done>Banner view renders the latest outage event and can be dismissed automatically.</done>
</task>

<task type="auto">
  <name>Task 2: Wire outage banner into chat layout</name>
  <files>Sources/DankChatApp/DankChatApp.swift</files>
  <action>
Insert the outage banner into the chat layout so it appears above the chat timeline on both phone and iPad layouts.
- Place the banner in `channelDetail(for:)` so it is visible when a channel is active.
- Keep the banner non-blocking and visually lightweight so it does not disrupt chat scrolling.
- Ensure the banner uses the `ProviderStatusStore` already injected into the environment.
  </action>
  <verify>Run: `swift build`</verify>
  <done>Recording an outage in `ProviderStatusStore` results in a visible banner/toast in the chat view.</done>
</task>

</tasks>

<verification>
- `swift build`
- Manual sanity check: trigger `providerStatusStore.recordOutage(...)` and confirm the banner/toast appears once.
</verification>

<success_criteria>
- Provider outages are visible to the user as a non-blocking banner/toast while chat continues to function.
</success_criteria>

<output>
After completion, create `.planning/phases/04-emotes-badges-ecosystem/04-08-SUMMARY.md`
</output>
