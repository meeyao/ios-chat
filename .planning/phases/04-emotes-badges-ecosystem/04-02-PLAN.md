---
phase: 04-emotes-badges-ecosystem
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - Sources/DankChatCore/Chat/ChatMessage.swift
  - Sources/DankChatCore/Chat/ChatUser.swift
  - Sources/DankChatCore/Chat/ChatMessageMapper.swift
  - Sources/DankChatCore/TwitchHelix/TwitchHelixClient.swift
  - Sources/DankChatCore/Emotes/Twitch/TwitchEmoteTagParser.swift
  - Sources/DankChatCore/Badges/Twitch/TwitchBadgeTagParser.swift
  - Tests/DankChatCoreTests/TwitchTagsParsingTests.swift
autonomous: true

must_haves:
  truths:
    - "Incoming IRC PRIVMSG events carry enough metadata to render Twitch emotes and badges"
  artifacts:
    - path: "Sources/DankChatCore/Emotes/Twitch/TwitchEmoteTagParser.swift"
      provides: "Parse Twitch IRC emotes tag into occurrences/ranges"
    - path: "Sources/DankChatCore/Badges/Twitch/TwitchBadgeTagParser.swift"
      provides: "Parse Twitch IRC badges tags into badge ids/versions"
    - path: "Tests/DankChatCoreTests/TwitchTagsParsingTests.swift"
      provides: "Unit tests covering emotes/badges tag parsing"
  key_links:
    - from: "Sources/DankChatCore/Chat/ChatMessageMapper.swift"
      to: "Sources/DankChatCore/Emotes/Twitch/TwitchEmoteTagParser.swift"
      via: "parse from IRC tags"
      pattern: "TwitchEmoteTagParser"
    - from: "Sources/DankChatCore/Chat/ChatMessageMapper.swift"
      to: "Sources/DankChatCore/Badges/Twitch/TwitchBadgeTagParser.swift"
      via: "parse from IRC tags"
      pattern: "TwitchBadgeTagParser"
---

<objective>
Extend the chat domain model to carry Twitch badge + emote metadata derived from IRC tags, and add tested parsing utilities.

Purpose: Accurate emote/badge rendering depends on IRC tag metadata (ranges for Twitch emotes; badge ids/versions for user badges).
Output: ChatMessage/ChatUser enriched with parsed tags; tag parsing is unit-tested.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-emotes-badges-ecosystem/04-CONTEXT.md
@.planning/phases/04-emotes-badges-ecosystem/04-RESEARCH.md

@Sources/DankChatCore/Chat/ChatMessage.swift
@Sources/DankChatCore/Chat/ChatUser.swift
@Sources/DankChatCore/Chat/ChatMessageMapper.swift
@Sources/DankChatCore/IRC/IRCMessage.swift
@Tests/DankChatCoreTests/IRCMessageParserTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed metadata for Twitch emotes + badges</name>
  <files>Sources/DankChatCore/Chat/ChatMessage.swift, Sources/DankChatCore/Chat/ChatUser.swift</files>
  <action>
Update chat models to carry emote/badge metadata needed by UI:

- Extend `ChatUser` to populate `id` from IRC tag `user-id` when present.
- Extend `ChatMessage` to include:
  - parsed Twitch emote occurrences/ranges (from IRC tag `emotes`)
  - parsed badge ids/versions (from IRC tag `badges`; optionally `badge-info` if needed)

Keep types simple, `Equatable`, and public so UI can render without reaching into raw IRC tags.
  </action>
  <verify>
Run: `swift build`
  </verify>
  <done>
`ChatMessage` instances created from IRC PRIVMSG can carry parsed `twitchEmotes` and `badgeTags` (even if empty).
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement + test IRC tag parsing for emotes and badges</name>
  <files>Sources/DankChatCore/Emotes/Twitch/TwitchEmoteTagParser.swift, Sources/DankChatCore/Badges/Twitch/TwitchBadgeTagParser.swift, Tests/DankChatCoreTests/TwitchTagsParsingTests.swift, Sources/DankChatCore/Chat/ChatMessageMapper.swift</files>
  <action>
Create parsing utilities:

- `TwitchEmoteTagParser`: parse the IRC `emotes` tag string into occurrences with `emoteId` and NSRange-like UTF-16 ranges (use `NSString` indexing to avoid Swift grapheme pitfalls).
- `TwitchBadgeTagParser`: parse the IRC `badges` tag (e.g. `moderator/1,subscriber/12`) into typed badge id/version pairs.

Wire parsing into `ChatMessageMapper.mapPrivmsg` so mapped `ChatMessage` carries parsed values.

Add `Tests/DankChatCoreTests/TwitchTagsParsingTests.swift` covering:
- empty tag strings
- multiple emote ids with multiple ranges
- overlapping/adjacent emote ranges
- badge parsing with multiple badges
  </action>
  <verify>
Run: `swift test --filter TwitchTagsParsingTests` (or `swift test` if filter is not supported).
  </verify>
  <done>
Unit tests pass and `ChatMessageMapper` populates the new metadata fields from IRC tags.
  </done>
</task>

</tasks>

<verification>
- `swift test` passes.
- A PRIVMSG line with `@badges=...;emotes=...;user-id=...` results in a `ChatMessage` with parsed fields populated.
</verification>

<success_criteria>
- The chat pipeline now produces events that can be rendered with Twitch emotes/badges without re-parsing raw tags in the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-emotes-badges-ecosystem/04-02-SUMMARY.md`.
</output>
