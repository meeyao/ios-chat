---
phase: 04-emotes-badges-ecosystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Package.swift
  - Sources/DankChatApp/DankChatApp.swift
  - Sources/DankChatApp/ImagePipeline/ImagePipeline.swift
autonomous: true

must_haves:
  truths:
    - "App configures image pipeline (WebP enabled) on launch"
  artifacts:
    - path: "Package.swift"
      provides: "SDWebImage + SDWebImageWebPCoder dependencies"
    - path: "Sources/DankChatApp/ImagePipeline/ImagePipeline.swift"
      provides: "Central SDWebImage/WebP configuration"
  key_links:
    - from: "Sources/DankChatApp/DankChatApp.swift"
      to: "Sources/DankChatApp/ImagePipeline/ImagePipeline.swift"
      via: "ImagePipeline.configure()"
      pattern: "ImagePipeline\\.configure\\(\\)"
---

<objective>
Add the standard image pipeline needed for emotes/badges: SDWebImage + WebP decoding, configured once at app launch.

Purpose: Phase 4 relies on WebP/GIF/APNG assets and aggressive caching; this establishes the supported image formats and cache behavior up-front.
Output: SPM dependencies added; `ImagePipeline.configure()` runs on startup.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-emotes-badges-ecosystem/04-CONTEXT.md
@.planning/phases/04-emotes-badges-ecosystem/04-RESEARCH.md

@Package.swift
@Sources/DankChatApp/DankChatApp.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SDWebImage + WebP coder via SwiftPM</name>
  <files>Package.swift</files>
  <action>
Update `Package.swift` to add SwiftPM dependencies:
- `https://github.com/SDWebImage/SDWebImage.git`
- `https://github.com/SDWebImage/SDWebImageWebPCoder.git`

Add products to the `DankChatApp` executable target (only where needed for UI rendering). Keep `DankChatCore` dependency-free unless a concrete need emerges.

Constraints:
- Do not introduce additional image libraries.
- Keep dependency versions compatible with iOS 16 and Swift tools 6.1.
  </action>
  <verify>
Run: `swift package resolve` and `swift build`
  </verify>
  <done>
`swift package resolve` completes, and the package builds with SDWebImage/WebPCoder linked to `DankChatApp`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Centralize image pipeline configuration</name>
  <files>Sources/DankChatApp/ImagePipeline/ImagePipeline.swift, Sources/DankChatApp/DankChatApp.swift</files>
  <action>
Create `Sources/DankChatApp/ImagePipeline/ImagePipeline.swift` exposing `ImagePipeline.configure()`.

In `configure()`:
- Register WebP coder (`SDImageWebPCoder`) with `SDImageCodersManager`.
- Configure SDWebImage cache defaults to reasonable limits (disk+memory) to prevent unbounded growth.

Wire `ImagePipeline.configure()` into `DankChatApp.init()` so it runs exactly once on launch.
  </action>
  <verify>
Run: `swift build`.
  </verify>
  <done>
App builds and WebP coder registration runs at startup (visible via code path, no runtime test required here).
  </done>
</task>

</tasks>

<verification>
- `swift build` succeeds.
- `Package.swift` contains SDWebImage + SDWebImageWebPCoder dependencies.
</verification>

<success_criteria>
- Image pipeline is configured on launch and ready for Phase 4 emote/badge rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/04-emotes-badges-ecosystem/04-01-SUMMARY.md`.
</output>
