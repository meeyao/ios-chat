---
phase: 04-emotes-badges-ecosystem
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - Sources/DankChatApp/UI/Chat/ChatMessageRow.swift
  - Sources/DankChatApp/UI/Chat/BadgeView.swift
  - Sources/DankChatCore/Badges/BadgeStore.swift
  - Sources/DankChatCore/Badges/BadgeVisibilitySettings.swift
  - Sources/DankChatApp/UI/Chat/ChatSettingsView.swift
autonomous: true

must_haves:
  truths:
    - "Badges render before usernames with Twitch-first ordering"
    - "Users can toggle visibility of badge categories/providers globally"
  artifacts:
    - path: "Sources/DankChatCore/Badges/BadgeVisibilitySettings.swift"
      provides: "Global, fine-grained badge visibility toggles"
    - path: "Sources/DankChatApp/UI/Chat/BadgeView.swift"
      provides: "Badge image + tap-to-name interaction"
  key_links:
    - from: "Sources/DankChatApp/UI/Chat/ChatMessageRow.swift"
      to: "Sources/DankChatCore/Badges/BadgeStore.swift"
      via: "badge resolution"
      pattern: "BadgeStore"
---

<objective>
Add badge rendering to chat rows (before username) and implement global, fine-grained badge visibility toggles.

Purpose: Badges are core parity for Twitch chat; users must be able to hide any badge types globally.
Output: Badge UI appears in message rows; settings can hide/show badges by provider/category.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-emotes-badges-ecosystem/04-CONTEXT.md
@Sources/DankChatApp/UI/Chat/ChatMessageRow.swift
@Sources/DankChatApp/UI/Chat/ChatSettingsView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement global badge visibility settings model</name>
  <files>Sources/DankChatCore/Badges/BadgeVisibilitySettings.swift</files>
  <action>
Create `BadgeVisibilitySettings` (ObservableObject) that supports:
- Global toggles (apply across all channels).
- Fine-grained selection across providers and badge categories.

Defaults locked by context:
- All badges visible by default.
- Unknown/new badge types default to visible.

Persist settings (prefer a simple persisted store such as UserDefaults) so choices survive app restarts.
  </action>
  <verify>Run: `swift build`</verify>
  <done>Settings object compiles and can answer "is badge X visible?" deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Render badges before username with correct ordering + tap behavior</name>
  <files>Sources/DankChatApp/UI/Chat/BadgeView.swift, Sources/DankChatApp/UI/Chat/ChatMessageRow.swift, Sources/DankChatCore/Badges/BadgeStore.swift</files>
  <action>
Create a `BadgeView` that renders a badge image and supports tap-to-show-name.

Update `ChatMessageRow`:
- Render all badges before username.
- Ordering locked by context: Twitch badges first, then third-party badges.
- Apply `BadgeVisibilitySettings` to filter badges.

Interaction locked by context:
- Tap badge: show badge name.
- Tap username (not badge): opens user card (wire to existing user card flow if available; otherwise add a placeholder hook/callback without implementing full card in this phase).
  </action>
  <verify>
Run: `swift build`.
Manual Xcode run: observe badges render before username; tapping a badge shows its name.
  </verify>
  <done>
Badges render in chat rows according to ordering rules, and badge tap reveals badge name.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expose badge toggles in settings UI</name>
  <files>Sources/DankChatApp/UI/Chat/ChatSettingsView.swift</files>
  <action>
Extend settings UI to surface badge visibility toggles.

Keep UI minimal but functional:
- Present grouped toggles by provider and/or category.
- Ensure all are toggleable (no hardcoded always-on).
  </action>
  <verify>Run: `swift build`</verify>
  <done>Badge toggles are reachable from the existing settings surface and update rendering.</done>
</task>

</tasks>

<verification>
- Manual run: disabling a badge category hides those badges across all channels.
</verification>

<success_criteria>
- Badge rendering parity path exists: badge images show before username and can be fully customized globally.
</success_criteria>

<output>
After completion, create `.planning/phases/04-emotes-badges-ecosystem/04-05-SUMMARY.md`.
</output>
